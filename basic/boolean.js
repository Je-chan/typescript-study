"use strict";
let isDone = false;
isDone = true;
console.log(typeof isDone);
let isOk = true;
// 아래 코드는 빨간 색 밑줄이 그려진다. 
// let isNotOk: boolean = new Boolean(true)
// 보통의 경우엔 직접 literal 값으로 primitive 타입을 적용한다.
